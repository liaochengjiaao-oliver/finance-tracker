# 个人记账 - 使用说明

## 安装与启动

**双击桌面上的「个人记账」图标即可启动。**

其他打开方式：
- **Spotlight**：按 `Cmd+空格` → 输入「个人记账」→ 回车
- **全局快捷键**：按 `Cmd+Shift+N` 可在任何时候弹出快速记账窗口（需应用已在后台运行）

**更新应用**（开发者）：修改代码后在项目目录运行 `npm run deploy`，会自动构建、打包、替换旧版本并安装。桌面快捷方式无需重建。

> 首次打开时 macOS 可能提示"无法验证开发者"，请前往 **系统设置 → 隐私与安全性**，点击「仍要打开」即可。

---

## 界面概览

应用左侧为导航栏，包含以下功能页面：

| 页面 | 说明 |
|------|------|
| 概览 | 首页仪表盘，展示本月收支概况 |
| 记账 | 新增收入或支出记录 |
| 导入 | 从支付宝/微信账单批量导入，管理分类映射规则 |
| 账单列表 | 查看、搜索、编辑、批量删除、导出记录 |
| 统计报表 | 图表分析收支数据，导出月度报告 |
| 年度账单 | 年度收支汇总与趋势分析 |
| 理财 | 记录理财本金与收益，查看汇总和趋势 |
| 分类管理 | 管理收支分类，备份/恢复数据 |

导航栏底部显示当前用户名，点击可切换用户。

---

## 功能详解

### 1. 概览页

打开应用后默认进入概览页，可以看到：

- **本月收入** — 绿色数字，附带与上月的对比百分比
- **本月支出** — 红色数字，附带与上月的对比百分比
- **本月结余** — 蓝色数字（收入 - 支出）
- **近期记录** — 最近 10 笔交易明细

### 2. 记账

点击左侧「记账」进入记账页面：

1. 选择类型：点击「支出」或「收入」按钮切换
2. 选择币种：默认人民币（¥ CNY），可切换为美元（$ USD）
3. 输入金额：支持小数点后两位（如 28.50）
4. 选择分类：从下拉菜单中选择（如餐饮、交通、工资等）
5. 选择日期：默认为当天，点击日历可修改
6. 填写备注：可选，最多 200 字
7. 点击「保存」完成记录

保存后页面不会跳转，可以继续记录下一笔。切换到其他页面再回来时，表单内容会保留。

### 3. 导入账单

支持从微信和支付宝导出的账单文件批量导入：

**支持的文件格式**
- 支付宝：`.csv` 文件（从支付宝 App 导出）
- 微信：`.xlsx` 或 `.csv` 文件（从微信支付公众号导出）

**使用步骤**
1. 点击左侧「导入」进入导入页面
2. 点击「选择账单文件」，选择你的账单文件
3. 系统自动解析并展示预览列表
4. 检查自动匹配的分类，如需修改可在下拉框中调整
5. 黄色标记的「可能重复」条目会排在最前面，鼠标悬停可查看已有记录详情
6. 取消勾选不想导入的记录
7. 点击「确认导入」完成

**自动过滤**
以下交易会被自动跳过，不进入预览列表：
- 转账、充值、提现、理财（不是真正的收支）
- 已退款的交易
- 因公付全额由公司承担的餐饮

**防重复检测**
- 如果检测到数据库中已有相同日期、金额、类型的记录，会标黄显示「可能重复」
- 悬停黄色标签可查看已有记录的详细信息（分类、备注等）
- 如果两笔交易时间相差超过 15 分钟，即使金额相同也不会标为重复

**自定义分类映射规则**
在导入页面展开「分类映射规则」面板，可以自定义关键词到分类的映射：
- 添加规则：输入关键词（如「星巴克」）+ 选择目标分类（如「餐饮」）
- 导入时系统会优先使用自定义规则匹配分类，未匹配的再走默认规则
- 可随时删除不需要的规则

**导入历史**
页面底部显示历史导入记录，包括导入时间、来源、条数和日期范围。

### 4. 快速记账（快捷键）

在任何应用中按下 `Cmd+Shift+N`，会弹出一个小窗口：

- 仅包含核心字段：类型、金额、分类、备注
- 日期自动使用当天
- 点击「保存」后窗口自动关闭
- 点击「取消」或点击窗口外部区域关闭

### 5. 账单列表

展示所有交易记录，提供丰富的筛选功能：

**快捷筛选**
- 点击「今天」「本周」「本月」「本年」按钮快速选择日期范围

**高级筛选**
- 日期范围：选择起止日期
- 类型：仅看收入 / 仅看支出
- 分类：可多选分类
- 金额范围：设置最小和最大金额
- 关键词：搜索备注内容

**批量操作**
- 勾选记录前的复选框可选中多条记录
- 点击「全选所有」可选中所有页的全部记录（不仅是当前页）
- 选中后可批量删除

**导出**
- **导出 CSV**：将当前筛选结果导出为 CSV 文件
- **导出 Excel**：导出带格式的 xlsx 文件（表头加粗、金额颜色区分、底部汇总行）

页面顶部始终显示当前筛选条件下的收入总计、支出总计和结余。

### 6. 统计报表

通过图表直观分析收支数据：

**月份选择** — 右上角月份选择器可切换查看不同月份

**分类占比（饼图）** — 各分类的占比，可切换收入/支出

**分类排名（柱状图）** — 按金额从高到低排列

**收支趋势（折线图）** — 收入和支出随时间的变化，支持按日/周/月/年切换

**分类趋势（柱状图）** — 选择一个分类，查看近 6 个月或今年各月的金额变化

**导出报告**
点击右上角「导出报告」按钮，可选择：
- **导出月度 Excel 报告**：包含收支概览、分类汇总、每日明细、交易记录 4 个 Sheet
- **导出月度 PDF 报告**：生成排版精美的 PDF 文件，包含收支概览、分类占比、每日收支表

### 7. 年度账单

年度收支汇总分析：

- 年度总收入、总支出、结余，以及与上年的对比
- 记账天数和笔数统计
- 支出最高月和最低月
- 月度收支趋势柱状图
- 分类占比饼图和排名柱状图

点击右上角「导出年度 PDF」可生成年度收支报告 PDF 文件。

### 8. 理财

管理理财资金的流入流出和收益：

**汇总卡片**：页面顶部展示当前本金、累计收益、理财总资产、年化收益率（XIRR）。

**记录操作**：
- **追加本金**：将资金投入理财
- **取出本金**：从理财中赎回资金
- **收益入账**：记录利息、分红等已到账收益

选择操作类型、输入金额、选择日期后点击「提交」即可记录。

**本金趋势图**：按月展示本金变化的折线图。

**收益趋势图**：月收益柱状图 + 累计收益折线图，直观展示收益增长。

**年度理财汇总**：
- 年度收益、本金变动、年化收益率、与去年收益对比
- 月度明细柱状图（追加/取出/收益按月展示）
- 点击「导出 PDF」可生成年度理财报告 PDF 文件

**操作记录**：所有操作按时间倒序展示，支持分页和删除。

### 9. 分类管理

管理收入和支出的分类标签：

**预设分类**

| 支出分类 | 收入分类 |
|---------|---------|
| 餐饮、交通、购物、娱乐、居住 | 工资、奖金、投资收益 |
| 通讯、医疗、教育、人情、其他 | 兼职、红包、其他 |

**自定义操作**
- **添加**：点击右上角「添加分类」，选择类型并输入名称
- **编辑**：点击分类右侧的铅笔图标修改名称
- **删除**：点击垃圾桶图标删除（如果该分类下有记录，需先将记录迁移到其他分类）

**数据备份与恢复**
页面底部的「数据管理」区域提供：
- **备份数据**：将当前用户的数据库文件导出到指定位置
- **恢复数据**：从备份文件恢复数据（会覆盖当前所有数据，操作前请确认）

---

## 语言切换

- 支持中文和英文两种界面语言
- 点击侧栏底部用户旁的语言按钮即可切换（按钮显示目标语言："EN" 表示可切换到英文，"中" 表示可切换到中文）
- 所有界面文字和预设分类名称（如 餐饮 → Food、交通 → Transit）都会跟随切换
- 语言偏好会自动保存，下次打开仍是上次选择的语言

## 多用户

- 支持多个用户，每个用户的数据完全独立
- 启动时自动登录上次使用的用户
- 侧边栏底部可切换用户或创建新用户

## 系统托盘

- 关闭主窗口时应用不会退出，而是最小化到系统托盘
- 右键点击托盘图标可以：快速记账、显示主窗口、退出
- 单击托盘图标可直接显示主窗口

## 明暗主题

应用自动跟随 macOS 外观设置，无需手动切换。

## 数据存储

- 所有数据保存在本机：`~/Documents/记账数据/users/<用户ID>/finance.db`
- 无需联网，不会上传任何数据
- 建议通过分类管理页的「备份数据」功能定期备份

## 快捷键汇总

| 快捷键 | 功能 |
|-------|------|
| `Cmd+Shift+N` | 快速记账（全局可用） |
| `Cmd+Q` | 退出应用 |
